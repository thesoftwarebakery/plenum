openapi: 3.1.0
info:
  title: CORS Example
  version: 1.0.0
paths:
  /products:
    get:
      operationId: listProducts
      responses:
        "200":
          description: List of products
  /no-cors:
    get:
      operationId: noCors
      responses:
        "200":
          description: No CORS configured
