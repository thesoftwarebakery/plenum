overlay: 1.1.0
info:
  title: Gateway configuration
  version: 1.0.0
actions:
  - target: $
    update:
      x-plenum-config:
        threads: 1
        listen: "0.0.0.0:6188"
      components:
        x-upstreams:
          default:
            kind: "HTTP"
            address: "backend"
            port: 8080
            buffer-response: true
  - target: $.paths[*]
    update:
      x-plenum-upstream:
        $ref: "#/components/x-upstreams/default"
