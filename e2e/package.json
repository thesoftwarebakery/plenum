{
  "name": "plenum-e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "pretest": "cargo run -p plenum-core --bin export_types --manifest-path ../Cargo.toml",
    "test": "vitest run --reporter=verbose",
    "bench": "tsx benchmarks/latency_bench.ts"
  },
  "devDependencies": {
    "@types/node": "^22",
    "mongodb": "^6.12.0",
    "mysql2": "^3.12.0",
    "pg": "^8.16.0",
    "testcontainers": "^11",
    "tinybench": "^6.0.0",
    "tsx": "^4",
    "vitest": "^3"
  }
}
