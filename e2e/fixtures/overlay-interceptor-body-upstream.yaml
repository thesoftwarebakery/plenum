overlay: 1.1.0
info:
  title: Wire upstream to wiremock (body test spec)
  version: 1.0.0
actions:
  - target: $
    update:
      x-plenum-config:
        listen: "0.0.0.0:6188"
      x-plenum-upstreams:
        default:
          kind: "HTTP"
          address: "wiremock"
          port: 8080
  - target: $.paths[*]
    update:
      x-plenum-upstream:
        $ref: "#/x-plenum-upstreams/default"
