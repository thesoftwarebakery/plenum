overlay: 1.1.0
info:
  title: Wire on_gateway_error interceptor that throws
  version: 1.0.0
actions:
  - target: $
    update:
      x-plenum-config:
        on-gateway-error:
          module: "./interceptors/on-gateway-error-throw.js"
          function: onGatewayError
