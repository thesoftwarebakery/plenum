overlay: 1.1.0
info:
  title: TLS test — add inbound TLS listener
  version: 1.0.0
actions:
  - target: $
    update:
      x-plenum-config:
        tls:
          cert-path: "/certs/gateway.crt"
          key-path: "/certs/gateway.key"
          listen: "0.0.0.0:6189"
